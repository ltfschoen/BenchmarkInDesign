#targetengine InDesign   var myDoc = app.activeDocument;$.writeln(myDoc);var currentSelectionName = app.selection[0].constructor.name;var currentSelection = app.selection[0];$.writeln(currentSelectionName);// check if rectangular frame selectedif (currentSelectionName == "Rectangle") {  $.writeln("current selection is a rectangle");  s1 = startTime();  // move the rectangle diagonally from top left to bottom right  for (i = 0; i < 50; i++) {    currentSelection.move([i,i]);     newDate = new Date();    $.writeln("frame top / left: " + currentSelection.geometricBounds[0] + " / " + currentSelection.geometricBounds[1]);  }  // call stopTime for finish time, passing reference to startTime  stopTime(s1); } else {  alert("please select the rectangle prior to commencing the test");} function startTime() { 	var d2 = new Date();	return d2;} function stopTime(s1) { 	$.writeln( s1.getTime() );  var t2 = Math.round(s1.getTime()/100);  var d1 = new Date();  $.writeln( d1.getTime() );  var t1 = Math.round(d1.getTime()/100);	var myTime =(t1-t2)/10;	if (myTime >= 60) { 		myMin = Math.floor( myTime/60 ); 		mySec = myTime-myMin*60; 		myTime = myMin + ':' + mySec + ' minutes.'; 	} else { 		myTime += ' sec.';	} 	alert( myTime );	 }